{
  "name": "flow",
  "private": true,
  "version": "1.8.3",
  "type": "module",
  "scripts": {
    "build:qrcode-vendor": "esbuild node_modules/qrcode/lib/browser.js --bundle --format=iife --global-name=QRCode --outfile=src/assets/vendor/qrcode.min.js --minify",
    "postinstall": "npm run build:qrcode-vendor",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "esbuild": "^0.28.0"
  },
  "dependencies": {
    "@phosphor-icons/web": "^2.1.2",
    "@tauri-apps/api": "^2.10.1",
    "@tauri-apps/plugin-updater": "^2.10.1",
    "qrcode": "^1.5.4"
  }
}
